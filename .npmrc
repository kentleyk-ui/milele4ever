# Allow sharp to run its build scripts (needed for image optimization)
side-effects-cache=true
shamefully-hoist=true

# Approve sharp package to run build scripts
# This fixes the "Ignored build scripts: sharp" warning
