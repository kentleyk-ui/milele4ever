# Ce fichier n'est utilisé que comme fallback si les variables ne sont
# pas déjà définies dans l'environnement (ex: build local sans `vercel env pull`).
# En production sur Vercel, les vraies valeurs sont configurées dans
# Project Settings > Environment Variables et prennent le pas sur ce fichier.
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key
GROQ_API_KEY=placeholder-key
